# Data and models (broad ignore - specific exceptions below)
data/
models/
*.tif
*.tiff
*.npy
*.npz
*.zip
*.pth
*.pt
*.ckpt
*.sec

# --- Exceptions: commit these specific small source files ---
# Each folder level must be un-ignored before the files inside it.

# Raw label CSV (the true input; the pairs file is regenerated from this)
!data/
!data/time_data_labeled/
!data/time_data_labeled/33648_A5_TS_dftcorr_02.csv

# Two small precomputed arrays (the 444 MB embeddings file stays
# ignored on purpose and is delivered via Google Drive instead)
!data/embeddings/
!data/embeddings/candidate_points_A5.npy
!data/embeddings/candidate_volume_indices_A5.npy



# External repositories
dinov3/

# Python
__pycache__/
*.pyc
*.pyo

# Environments
venv/
.venv/
.env/

# Experiment logs
logs/

# Outputs
outputs/
results/

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db